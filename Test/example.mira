#define Abc(a,b,c) local a = function(d) return c+b-(c*c) end
#define Match(f,d,...) f#d + f k##__VA_ARGS__

local a = t -> t:GetName()
local c = (a, b) -> a == b

local a = fun {$1==$2}
local b = fun {it.count}

local a,b = {>},{not}

if local target = a:GetTarget() then
  ShootAt(target)
elif local health = f:GetHealth() then -- elif equals to elseif
  print(health)
end

~~a
++a.b()[1]:c()[2]
Abc(mf,10,3-3)
mf(3)
-- Match(1,teach{}, {{{}}}, "*#")
